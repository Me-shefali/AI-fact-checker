# Change the username, password, and database name as per your setup.
# Keep the format same as shown below.
DATABASE_URL=postgresql://username:password@localhost:5432/factchecker
SECRET_KEY=your_secret_key_here
ALGORITHM=algorithm_used_for_token_generation